import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  avatarImage: {
    width: '100%',
    height: '100%',
    borderRadius: 16,
    resizeMode: 'cover',
  },
  addBtn: {
    position: 'absolute',
    right: -14,
    bottom: 14,
    borderRadius: 45,

    backgroundColor: '#ffffff',
  },
});
